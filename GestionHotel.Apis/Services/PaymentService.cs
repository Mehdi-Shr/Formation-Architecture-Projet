public class PaymentService {
    public bool ProcessPayment(ClientModel client, decimal amount) {
        // Logique de traitement des paiements (simulée pour cet exemple)
        return true;
    }
}